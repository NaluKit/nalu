Das Parameter-handling besser machen.


1. Parameter ermitteln und mit ID in HashMap
2. Zu viele Parameter in URL ignorieren
3. Paramter in Reihenfolge auf ids aus der Route mappen
4. Controller nach set-Methode suchen, wenn gefunden übergeben, ansonten igonorieren
5. in child routes nach parametern aus map suchen und aufrufen

Info-Messages ausgeben, wenn keine set-Methode implementiert ist controller: blablabla does not accept parameter bla